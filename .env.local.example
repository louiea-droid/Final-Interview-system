# Local development credentials for the admin sign-in.
#
# These are NOT security - they sit in the browser bundle. They exist so the
# admin screens can be used while everything runs locally. Both are optional;
# the defaults below apply when unset.
VITE_LOCAL_ADMIN_EMAIL=admin@local
VITE_LOCAL_ADMIN_PASSWORD=admin
